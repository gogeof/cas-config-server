logging:
  file: "logs/cas-config.log"
info:
  name : "central of configure"

---
server:
  port: 8888
  context-path: /config
spring:
  profiles:
    active:
      - native
  application:
    name: sso-config